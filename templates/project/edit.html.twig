{% extends 'base.html.twig' %}

{% block title %}Edit Project{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-9">
            <h1>Modifier le projet : {{ project.name }}</h1>
        </div>
        <div class="col-md-3 text-right">
            <a href="{{ path('project_index' , {'id' : project.client.id}) }}" class="btn btn-primary">Retour à la
                liste</a>
            <a onclick="return confirm('Are you sure you want to delete this item?');"
               href="{{ path('project_delete', {'id': project.id}) }}"
               class="btn btn-sm btn-danger"><i class="fa fa-1x fa-trash"></i> </a>
        </div>
    </div>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}




{% endblock %}
