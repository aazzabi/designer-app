{% extends 'base.html.twig' %}

{% block title %}Edit Project{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-9">
            <h1>Modifier le projet : {{ project.name }}</h1>
        </div>
        <div class="col-md-3 text-right">
            <a href="{{ path('project_index' , {'id' : project.client.id}) }}" class="btn btn-primary">Retour à la liste</a>
            <btn class="btn btn-danger btn-sm">{{ include('project/_delete_form.html.twig') }}</btn>
        </div>
    </div>

    {{ include('project/_form.html.twig', {'button_label': 'Cofirmer'}) }}



{% endblock %}
